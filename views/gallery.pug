extends layout

block layout-content
    h2 #{title}
    h3 #{description}
    div.thumbnail-container
        ul.thumbnails
            //- each photo in photos
            //-     li photo
    a(href='/gallery/create') New Gallery